# price-check

英國超市比價 PWA（Cloudflare Workers + D1 + R2 + Workers AI），部署在作者自己的網域、僅供個人使用。

使用者住在英國，在 Sainsbury's / Waitrose / Co-op / Aldi / Lidl 之間採買，
想知道「這東西哪家便宜」與「這一籃去哪家最省」。
產品全貌與各階段進度見 [README.md](README.md)。

---

## 已經定案的事，不要再問也不要改回去

| 決定 | 原因 |
|---|---|
| **零依賴、原生 JS** | 不引入任何前端框架或第三方函式庫（含圖表庫）。PWA 要在超市裡瞬開，每 KB 都算。 |
| **極簡黑白 + 蜜柑橘 `#E8590C`** | 跟 travel-money 同一套視覺語言。白字壓橘底時改用 `--accent-ink: #CC4D0A`，因為 `#E8590C` 配白字對比只有 3.58，過不了 WCAG AA。 |
| **淺色為主、深色跟隨系統** | 兩套 CSS 變數都要維護。 |
| **中英並列，不做語言切換** | 中文回答「這是什麼」，英文回答「在店裡怎麼找」。兩個同時要，不是二選一。 |
| **超市名不翻譯** | 招牌上長什麼樣就長什麼樣。翻成「森寶利」在店裡對不上。這是中英並列唯一的例外。 |
| **AI 辨識結果一律進確認卡** | 不直接寫進資料庫。 |

## 架構上不能破壞的東西

**價格用整數便士。** `prices.price_pence` 是 INTEGER，£1.45 存成 `145`。
籃子比價要做大量加總與排序，浮點誤差在「總價差 3p」這種場景會直接騙人。
單價是推算出來的（145 ÷ 2000ml），它保持浮點，只在顯示與最後加總時才 `Math.round`。
不要把單價也湊成整數 —— `£0.0725/ml` 會全部塌成 0，比價就沒得比了。

**items / products / prices 三層不能壓平。** 不分層就沒辦法比價：
Aldi 2L £1.45 對上 Waitrose 1.13L £1.35，直接比標價會得到相反的結論。
`items.base_unit`（g / ml / each）是所有換算的根，建立後不要改 ——
底下所有商品的容量單位都是照它填的。

**缺資料不補值。** 籃子比價時某家店缺某個品項，不拿別家價格補、也不當成 0。
排行**先比涵蓋率再比總價**（`basketAnalysis()`），並列出缺哪幾項。
偷偷補值會讓資料最少的那家永遠看起來最便宜，剛好是反的。
「只比共同品項」開關才是真正公平的比較，但它常常會是空的，所以只能當開關不能當預設。

**店家分組要分開比。** `stores.js` 的 `group`（`uk` / `asian` / `indie`）不是裝飾，
單品排行（`rankByGroup`）與籃子總價（`basketAnalysis(groupId)`）都是分組算的。
不要為了「看起來比較簡潔」把它們合併回一個排行 —— 你沒辦法在 Aldi 買醬油、
蔬果行也沒有衛生紙，混排只會得到「Tian Tian 的牛奶比較貴」跟
「只賣少數品項的店永遠墊底」這兩種沒有用的結論。

**加分組只改 `GROUPS`。** 排行、籃子、店家列、店家編輯的下拉選單全都照著它跑。
唯一要一起維護的是 `normGroup()`（未知組別一律歸 `uk`）—— 判斷組別時請用它，
不要再寫 `g === "asian" ? … : "uk"` 這種只認得兩組的三元式。

**內建店家寫在程式裡，自訂店家放 D1。** `stores.js` 的 `BUILTIN` 是常數，
使用者自己加的走 `stores` 同步表，`setCustomStores()` 在每次 `reload()` 後把它們
推進 stores.js 的登錄表（畫面到處同步呼叫 `storeName()`，所以不能是 async）。
**自訂店家一定要同步** —— 不然另一台裝置上的價格會顯示不出店名。
`storeById/storeName/storeColor` 查的索引**包含已軟刪除的店**，
因為歷史價格還掛在它們身上，顯示成 UUID 等於資料壞掉。

**台幣不凍結，而且不准參與計算。** `fx.js` 一律用目前匯率換算，跟 travel-money
刻意相反（那邊是記帳，必須凍結才對得起來；這邊問的是「現在貴不貴」）。
台幣只做顯示、一律加 `≈`、不進任何排序或加總。要改成凍結請先想清楚為什麼。

**匯出的比價邏輯不要在 `export.js` 裡重寫。** `rankItem` 由 app.js 傳進去。
匯出的數字跟畫面上看到的不一樣，是這種工具最難發現、也最傷信任的 bug。

**IndexedDB 是唯一真相來源。** UI 只讀寫本機，永遠不等網路。同步是背景行為。
不要改成「送出 API 成功才更新畫面」—— 超市裡收不到訊號是常態，
而記價就是要站在貨架前面做。照片同理：先存本機，之後再排隊上傳 R2。

**同步靠 `server_seq` 不靠時間戳。** 伺服器發單調遞增號碼，客戶端記住位置往前拿。
衝突才用 `updated_at` 判斷（單人使用，衝突極少）。

**不要加外鍵。** 同步時 `prices` 可能比 `products` 先到，有外鍵會直接被擋。
完整性由應用層負責。

**加了新的 IndexedDB object store 一定要進 `DB_VERSION`。** 不進版就不會觸發
`onupgradeneeded`，已經在用的裝置一開 transaction 就 NotFoundError，整個 App 掛掉。
（v2 = 加入 stores。）

**刪除一律軟刪除。** 留一列帶 `deleted_at`，否則刪除同步不到其他裝置。

**不要用 `navigator.onLine` 判斷能不能用。** 超市地下室最常見的狀況是
「連得上 Wi-Fi 但出不去」，這時 `onLine` 是 true 而 fetch 直接失敗。
`boot()` 改用 meta 裡的 `signed_in` 旗標決定要不要放人進去；
cookie 真的失效時，背景同步會回 401，那時才要求重新登入。

**照片來源不要全部加 `capture`。** `<input type="file" accept="image/*" capture="environment">`
在手機上會直接開相機，相簿裡已經拍好的照片就永遠選不到了。
只有「拍標籤」該加 `capture`；「選照片」與「批次匯入」不加，iOS 才會給
照片圖庫／拍照／瀏覽 的選單。這是實際踩到的問題。

**批次佇列要落地。** `queue` 存在 meta 的 `photo_queue`，不是只放記憶體。
匯入 20 張走到第 12 張時手機切掉、App 被系統回收是常態，回來要能接著做。
照片本身在匯入的當下就寫進 IndexedDB 了，佇列只是記「走到第幾張」。

**日期用當地日期。** `prices.observed_on` 存當地 `YYYY-MM-DD`（`units.localDate()`）。
晚上 11 點在 Sainsbury's 拍的標籤不該被算成隔天。

**新增與編輯共用 `openRecord()`。** 傳 `editPriceId` 就是編輯模式。
不要為了「編輯畫面比較簡單」另外寫一份表單 —— 欄位一模一樣，兩份遲早會走鐘。

**編輯時不可以自動跑辨識。** `attachStoredPhoto(el, draft, { autoRead })`：
批次匯入才 `autoRead: true`。編輯時自動辨識會把使用者正要修正的值
用 AI 的猜測蓋掉，而且他不會發現。

**改規格＝重新掛商品，不是原地改。** 編輯時改了容量／超市／品項，走的是跟
`saveRecord` 一樣的 find-or-create，然後把價格 re-point 過去、舊商品沒價格就軟刪除。
不要改成直接改 `products` 那一列 —— 那會連帶改掉同一個商品底下所有歷史觀測的單價。

**`items.base_unit` 在有商品之後要鎖住。** `openItemEditor` 會依
`productsOfItem().length` 把下拉選單 disabled。改掉會讓 500g 被當成 500ml，
所有單價默默算錯而且畫面上看不出來 —— 這是最惡劣的一種 bug。

**比價與清單是同一頁，不要再拆開。** 它們做的是同一件事的兩半（都在列品項），
拆開之後使用者得在比價頁找到東西、記住它、再去清單頁加一次。
勾選存的仍然是 `list_items`（份量、同步、跨裝置全部照舊），只是入口變成首頁的圈圈。

**品項列是兩個並排的按鈕，不是一個大按鈕。** 左邊圈圈勾選、右邊開明細 ——
按鈕不能巢狀在按鈕裡面。選單裡（`openItemPicker`）不傳 `pick`，整列都是 open。

**店家卡片要自己回答問題，不能只給數字。** 卡片直接列出最划算的前 `CARD_WINS` 樣
與省多少。使用者的原話是「要一眼就看得出那個超市裡面買什麼最划算」——
站在店門口滑手機，多按一下就是多一次分心。不要為了版面整齊改回只顯示計數。

**「只有這裡有紀錄」不能算成勝場。** `storeReport()` 把沒有對照組的品項放進
獨立的 `only` 桶。一家店是唯一有紀錄的，不代表它便宜，只代表沒人去別家記過 ——
算成「贏」會讓使用者以為那裡划算，這是拿沒有的資訊騙人。

**圖示是三層解析，不是綁在分類上。** `itemEmoji()`：自己挑的 → `emoji.js` 從名稱猜
→ 分類的。分類只有十幾個但買的東西有幾百種，一對一永遠對不上（蘋果跟青江菜都是蔬果，
但不該長一樣）。`emoji.js` 的關鍵字表**要先寫具體的**：「雞胸肉」排在「雞」前面、
「牛奶」排在「牛」前面，否則會被吃掉；`蛋$` 這種結尾的 `$` 代表整個名稱剛好等於它，
不然「蛋餅皮」會被「蛋」吃掉。

**分類不自動改，要讓人逐條確認。** `openTidy()` 只**提議**，勾選才寫入。
分類是使用者的判斷不是我們的 —— 「蛋」在英國超市歸乳製品區、「豆腐」放主食
還是蛋白質都合理。圖示則不需要寫入，它每次都從名稱重新解析。

**孤兒商品要收掉。** 刪掉一個商品的最後一筆價格時，把商品一起軟刪除。
留著一個沒有價格的商品，畫面上完全看不到它，卻會在下次記價時被當成
「上次的規格」帶出來，害你存出一筆沒有容量、算不出單價的紀錄。

## 各頁職責

| 頁面 | 回答什麼問題 |
|---|---|
| 比價（首頁） | 搜尋品項 → 點名字看單品哪家便宜 → 勾圈圈加進整籃，總價面板長在同一頁上 |
| 記價（FAB） | 站在貨架前面，怎麼用最少的動作把價格存下來 |
| 店家 | 我人在這家店，有什麼值得買（勝場計分板 → 划算／獨有／別買） |
| 紀錄 | 我剛剛記了什麼、點進去修 |
| 設定 | 同步、照片批次匯入、匯出、辨識模型、模型比對、本機照片、外觀 |

記價流程的原則是**任何一步都可以跳過**：沒拍照可以記、沒選品項可以當場建、
沒網路可以存。站在貨架前面卡住是最糟的事。不要為了塞欄位破壞這件事。

## 檔案

```
src/index.js        Worker：登入、/api/sync、照片 R2 存取、辨識端點
src/extract.js      標籤辨識：模型清單、prompt、回應正規化、bakeOff()
schema.sql          D1 結構（7 張同步表 + fx_rates 匯率快取）
                    既有 DB 加欄位要自己下 ALTER，CREATE TABLE IF NOT EXISTS 不會補
public/app.js       畫面與互動（單一檔案，照區塊註解分段）
public/db.js        IndexedDB + 同步佇列 + 照片上傳佇列
public/units.js     單位換算、單價計算、金額格式化、日期新鮮度
public/i18n.js      中英並列字串表與 bi()/bi1()/biText() 排版 helper
public/stores.js    內建店家常數 + 自訂店家執行期登錄、分組（一般／亞洲／獨立）
public/emoji.js     名稱→圖示／分類的關鍵字表（具體的要排前面）與挑選面板清單
public/fx.js        GBP→TWD（參考值，不凍結；不要拿去排序或加總）
public/export.js    CSV 與 xlsx 產生器（零依賴，自己打包 zip；不要換成 SheetJS）
public/app.css      樣式（CSS 變數在最上面）
public/sw.js        Service Worker
```

## 版控

**每次改完要 commit**，
訊息用中文、寫「為什麼改」而不是「改了什麼」。

部署目前是手動 `npx wrangler deploy`，沒有接 CI。

## 開發

```bash
npm install
cp .dev.vars.example .dev.vars
npx wrangler d1 execute price-check --local --file=./schema.sql
npm run dev
```

**改了 `public/` 卻沒生效，先懷疑 Service Worker。** 它是快取優先。清掉最快：

```js
for (const r of await navigator.serviceWorker.getRegistrations()) await r.unregister();
for (const k of await caches.keys()) await caches.delete(k);
location.reload();
```

用 `fetch(url, {cache:"reload"})` 手動塞快取是**沒用的** —— 那個請求本身也會被 SW
攔下來回舊的，等於把舊檔又寫回快取一次。這個坑踩過一次了。

改了 `public/` 的檔案清單記得同步更新 `sw.js` 的 `SHELL` 陣列，並把 `VERSION` 進版。

**Workers AI 在 `wrangler dev` 底下是遠端 binding**，會真的打 Cloudflare 並消耗
Neurons（免費額度每天 10,000）。沒登入 wrangler 的話 `/api/extract` 會失敗，
但 App 其餘部分照常運作 —— 手動輸入永遠不依賴 AI。

## 驗證的標準

每次改動都實際跑過再說「完成」。這個專案已經驗證過的方式：

- **金額計算**：手算期望值逐一對照，不是「看起來合理」。
  特別注意**顯示精度不能影響排序** —— 72.5p/L 與 72.6p/L 都顯示成 `73p/L`，
  但排行必須仍然正確
- **籃子總價**：手算整籃期望值對照 UI；刻意讓某店缺一項，
  確認顯示「已知 2/3」而不是偷偷補值，而且它不會因為數字小就排到前面
- **離線**：**把 dev server 整個停掉**再記帳、再重新載入頁面，
  不是只看 `navigator.onLine`
- **同步**：把本機 IndexedDB 整個刪掉重開，確認能從 D1 完整重建
- **照片**：確認 R2 真的收到（GET 回 200 且位元組數相符），不是只看前端沒報錯
- **版面**：量測 375×812 與 1280×800，檢查橫向溢位與**獨立**點擊區 ≥ 44px
  （嵌在 `.row` 裡的 input 不算，整列就是它的點擊區）

不要只做靜態檢查就回報完成。做不到的部分要明講沒跑過，不要說「已驗證」。

## 語言

使用者說中文，回覆用繁體中文。程式碼註解也用繁體中文，寫「為什麼」不寫「做什麼」。
UI 文案中英並列。
